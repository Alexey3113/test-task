import React from "react";
import { Main } from "../components";

const MainPage: React.FC = () => {
    return (
        <div>
            <Main />
        </div>
    );
};

export default MainPage;
